GeneratorModel for create::java {

	const PROJECT : string = "CDPlayer"

	const FOLDER : string = "src"

	statechart CDPlayer {

		feature Outlet {
			targetProject = PROJECT
			targetFolder = FOLDER
		}
		
		feature Naming {
    		basePackage = "CDPlayer"
	    	typeName = "GeneratedStatechart"
		}
	}
}